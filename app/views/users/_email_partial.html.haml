.col-sm-5
    %p.in-gold-500.font-18.mb-25= t('form_settings.email_edit')
    = simple_form_for(current_user, url: user_path, method: :put, defaults: {input_html: {class: "form-control"}}) do |f|
        .form-group.mb-35
            = f.input :email, placeholder: t('form_settings.email_placeholder')
        .general-text-align
            = f.button :submit, t('form_settings.email_button'), class: 'btn btn-default mb-35', id:'saveEmail'
